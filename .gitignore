# Local inference config (copy from inference.env.example; never commit the real key)
inference.env
.env

# Track data (mounted, never committed)
data/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db
